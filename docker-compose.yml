services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ntd-backend
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/app/data/ecommerce.db
      - PORT=8080
    volumes:
      - sqlite-data:/app/data

volumes:
  sqlite-data:
